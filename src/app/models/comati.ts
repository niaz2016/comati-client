export type Comati = {
    id: number,
    name: string,
    start_date: string,
    end_date?: string,
    per_head?: number,
    remarks: string,
    managerId: number,
    totalMembers: number,
    totalComati: number,
}